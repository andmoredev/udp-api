# ANDRES MORENO

name: Copyright Validation

on:
  push:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  validate-copyright:
    runs-on: ubuntu-latest
    steps:
    - name: Validate Copyright
      uses: ./.github/actions/validate-copyright